/**
 * @acme/components - 通用 UI 组件库
 *
 * 此包包含与业务逻辑无关的通用 UI 组件，可在多个应用间复用。
 *
 * 放置原则：
 * - 通用 UI 组件（Button、Modal、Card 等封装）
 * - 布局组件（Container、Grid、Stack 等）
 * - 表单组件（FormField、Select、DatePicker 封装等）
 * - 展示组件（Avatar、Badge、Tag 等）
 * - 工具组件（Loading、Empty、ErrorBoundary 等）
 *
 * 不放置：
 * - 包含业务逻辑的组件
 * - 依赖特定 API 或 tRPC 的组件
 * - 仅在单一页面使用的组件
 */

// TODO: 从 web/components 中抽取通用组件到此处
// export * from "./Button";
// export * from "./Modal";
// export * from "./Card";
// export * from "./Loading";
// export * from "./Empty";
